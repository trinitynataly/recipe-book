/*
Version: 1.1
Last edited by: Natalia Pakhomova
Last edit date: 03/06/2024
PostCSS configuration file for the Recipe Book application.
*/

/** @type {import('postcss-load-config').Config} */
// Define PostCSS configuration object
const config = {
  plugins: { // Define the PostCSS plugins
    tailwindcss: {}, // Use the Tailwind CSS plugin
  },
};

// Export the PostCSS configuration object
export default config;
